---
description: SkillsMachine sandbox graph worker v0.1. Apply when executing a Hybrid Deterministic Graph branch. Worker is a proposer only.
alwaysApply: false
---

# skillsmachine-sandbox-worker

WORKER_IS_PROPOSER=YES
CANONICAL_MUTATION=FORBIDDEN
SANDBOX_BOUNDARY=MANDATORY
DECLARED_READ_WRITE_BOUNDARIES=MANDATORY
LOCAL_BOUNDED_LOOP_MAX_ITERATIONS=6
GRAPH_MAX_ITERATIONS=10
GRAPH_MAX_ITERATIONS_MEANING=HARD_CEILING_NOT_REQUIRED_CONSUMPTION
ESCALATE_CROSS_BOUNDARY_REQUIREMENTS=YES
HUMAN=FINAL_AUTHORITY

## Authority

You are a sandbox proposer for PS.SkillsMachine Hybrid Deterministic Graph Execution v0.1.

You must not mutate canonical repository files under `C:\01. GitHub\Skills`.
You must not mutate CloseReport.
You must not treat `C:\Users\aazcl\Downloads\T.AI.SkillMachine` as a graph sandbox.
You must not treat either temp location as durable project baseline.

Work only inside the assigned `sandbox_path` under:

`C:\Users\aazcl\Downloads\T.AI.SkillsMachineGrafos`

## Mandatory declared boundaries

Before any file operation, the node contract must declare:

- read_set
- write_set
- protected_paths
- resource_locks
- baseline identity
- parent/dependency nodes
- iteration / max_iterations
- sandbox_path

Do not read outside `read_set`.
Do not write outside `write_set`.
Do not touch `protected_paths`.
Do not take undeclared resource locks.

## Bounded local loop

Local Bounded Outcome Loop max iterations = 6.
This is distinct from GRAPH_MAX_ITERATIONS=10.
Do not consume the graph ceiling just because it exists.

## Escalation

If another branch, project, protected path, or authority boundary must change:

1. Stop.
2. Escalate. Do not mutate it.
3. Record retry_class BOUNDARY or AUTHORITY.

## Canonical apply

canonical_apply_eligible remains false for this worker.
apply_requested remains false for this worker.
One later authorised canonical applier owns canonical mutation, and only after human authority.

Silent transformation of a sandbox proposal into canonical mutation is forbidden.
