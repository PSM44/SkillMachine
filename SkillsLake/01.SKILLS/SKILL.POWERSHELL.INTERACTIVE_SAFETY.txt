
==========
SKILL.POWERSHELL.INTERACTIVE_SAFETY
==========

ID_UNICO..........: SKILL.POWERSHELL.INTERACTIVE_SAFETY
NOMBRE............: PowerShell Interactive Safety
TIPO..............: Skill operativo
DOMINIO...........: PowerShell / Windows / CLI execution
ESTADO............: ACTIVE
VERSION...........: 1.3
FECHA.............: 2026-07-19
ORIGEN............: Lessons learned from MB-GRC-014A / SkillsLake migration session; updated after MB-EXAM-005R2 PowerShell wrapper recovery; repaired by MB-SKILL-PS-INTERACTIVE-002; hardened by MB-SM-067B-D2 with StrictMode/array/parser anti-patterns

----------
1. PURPOSE
----------

This skill defines safe execution practices for PowerShell commands provided to a human operator in an interactive terminal.

The objective is to reduce syntax errors, partial-copy failures, execution drift, and avoidable interruptions when the user copies and pastes command blocks into PowerShell.

----------
2. PROBLEM STATEMENT
----------

During operational sessions, the following recurring error was observed:

else: The term 'else' is not recognized as a name of a cmdlet, function, script file, or executable program.
Check the spelling of the name, or if a path was included, verify that the path is correct and try again.

Root cause:
PowerShell interpreted `else` as a standalone command because the prior `if` block had already been closed and executed.

This often happens when a user copies a script in fragments, or when the interactive shell receives the `if` block and the `else` block as separate instructions.

----------
3. CORE RULE
----------

When writing PowerShell instructions for interactive copy/paste execution, avoid `else` unless the full `if/else` structure is guaranteed to be pasted and executed as one atomic block.

Preferred pattern:

if (Test-Path $Path) {
  Write-Host "Exists"
}

if (-not (Test-Path $Path)) {
  Write-Host "Missing"
}

Avoid this pattern in fragile interactive contexts:

if (Test-Path $Path) {
  Write-Host "Exists"
}
else {
  Write-Host "Missing"
}

The second pattern is valid PowerShell, but it is less robust when copied manually into an interactive console.

----------
4. STANDARD EXECUTION HEADER
----------

Every non-trivial PowerShell one-shot should include:

$ErrorActionPreference = "Stop"

Recommended minimal header:

cd "C:\TARGET\PATH"

$ErrorActionPreference = "Stop"

Write-Host "========== TASK NAME =========="

----------
5. LOCATION FIRST RULE
----------

Before providing a command, always state the execution location.

Example:

Location:
C:\01. GitHub\Skills

Command:
cd "C:\01. GitHub\Skills"

This prevents accidental execution in the wrong repository or directory.

----------
6. NO-ELSE INTERACTIVE PATTERN
----------

Use independent `if` blocks:

if (Test-Path $Source) {
  Copy-Item $Source $Target -Force
  Write-Host "Copied:"
  Write-Host $Source
}

if (-not (Test-Path $Source)) {
  Write-Host "Source not found:"
  Write-Host $Source
}

This pattern is more verbose but safer for human copy/paste workflows.

----------
7. PATH SAFETY
----------

Always quote Windows paths.

Correct:

cd "C:\01. GitHub\Skills"

Incorrect:

cd C:\01. GitHub\Skills

Reason:
Spaces in paths break unquoted commands.

----------
8. ERROR HANDLING
----------

Use:

$ErrorActionPreference = "Stop"

This turns many non-terminating PowerShell errors into terminating errors.

For critical file operations, validate before and after:

if (Test-Path $Target) {
  Write-Host "OK:"
  Write-Host $Target
}

if (-not (Test-Path $Target)) {
  throw "Missing expected target: $Target"
}

----------
9. COMMAND OUTPUT DISCIPLINE
----------

Each one-shot should produce visible section headers.

Recommended format:

Write-Host "========== PRECHECK =========="
Write-Host "========== EXECUTION =========="
Write-Host "========== VALIDATION =========="
Write-Host "========== STATUS =========="

This helps the AI and the human operator identify where an error occurred.

----------
10. HIGH-RISK COMMANDS
----------

The following commands require extra caution:

Remove-Item
Move-Item
git reset
git clean
git add .
git commit --no-verify
npm uninstall -g
Set-Content
Copy-Item -Recurse -Force

Rules:
- State the target path before execution.
- Back up first when relevant.
- Avoid wildcards unless validated.
- Prefer explicit path lists over broad patterns.
- Never use destructive commands without a status check first.

----------
11. BACKUP RULE
----------

Before modifying, deleting, or replacing operational files:

1. Create external backup.
2. Confirm backup path.
3. Perform change.
4. Validate result.
5. Show git status if inside a repo.

Preferred backup location examples:

C:\Users\aazcl\Desktop\<TASK>_BACKUP_<YYYYMMDD>
C:\00. Wings\99.Backups\<PROJECT>\<TASK>_<YYYYMMDD_HHMMSS>

Avoid placing temporary backups inside the repository unless explicitly intended.

----------
12. GIT REPO SAFETY
----------

Before modifying files inside a Git repository:

git status --short

After modifying files:

git status --short

Before commit:

git diff --cached --name-status
git diff --cached --stat

Never assume the working tree is clean.

----------
13. KNOWN ANTI-PATTERNS
----------

Anti-pattern 1:
Providing only a command without execution location.

Anti-pattern 2:
Using `else` in commands likely to be copied in fragments.

Anti-pattern 3:
Using `git add .` during mixed-change states.

Anti-pattern 4:
Creating temporary diagnostic files inside the repo and forgetting to remove them.

Anti-pattern 5:
Running destructive commands before inventory.

----------
14. ACCEPTANCE CRITERIA
----------

A PowerShell command block complies with this skill when:

1. It states the working directory.
2. It uses quoted paths.
3. It uses `$ErrorActionPreference = "Stop"` for non-trivial operations.
4. It avoids fragile interactive `else` blocks.
5. It prints clear section headers.
6. It validates critical results.
7. It avoids broad destructive actions.
8. It shows final status when operating inside Git.

----------
15. LESSON LEARNED
----------

PowerShell scripts written for a file and PowerShell commands written for interactive copy/paste are not the same product.

For human-guided AI operations, robustness beats elegance.

========== SKILL UPDATE MB-EXAM-005R2 POWERSHELL WRAPPER SAFETY BEGIN ==========

----------
16. LESSON LEARNED - HERE-STRING AND WRAPPER FILE SAFETY
----------

A second recurring failure mode was observed during MB-EXAM-005 / MB-EXAM-005R / MB-EXAM-005R2:

- A long PowerShell wrapper was pasted into an interactive console.
- The wrapper used an outer here-string to write a .ps1 file.
- Inside that wrapper, another here-string was used to define Python code.
- PowerShell interpreted the inner terminator as the end of the outer here-string.
- Everything after that point executed loose in the console.
- Variables such as ProducerPath, ReportPath, RegisterPath and LogPath became null.
- Functions such as Invoke-NativeChecked were not available.
- try, catch and finally blocks were executed as orphan commands.
- A manual STATUS OK line was printed even though the operation had failed.

Root cause:

PowerShell here-strings are delimiter-sensitive. Nested here-strings using the same delimiter pattern are unsafe when generating scripts interactively. This is worse when the human operator pastes a large block into the console instead of executing a saved .ps1 file.

Operational conclusion:

A script that works as a file is not automatically safe as an interactive paste block. Delivery format is part of the design.

----------
17. CORE RULE - DO NOT NEST FRAGILE HERE-STRINGS IN INTERACTIVE WRAPPERS
----------

Avoid this in interactive wrappers:

Outer wrapper:
@'
...
Inner generated script:
ProducerCode = @'
...
'@
...
'@ | Set-Content ...

This can prematurely close the outer block and cause partial execution.

Preferred options:

Option A - Write the final .ps1 file directly, then execute it.
Option B - Use a different content construction method, such as an array of lines joined with CRLF.
Option C - Use base64 payload only when justified and clearly validated.
Option D - Use double-quoted here-string inside single-quoted outer wrapper only if there is no variable expansion risk and delimiters are visually audited.

Recommended default for AI-guided human execution:

1. Create a script file under Temp.
2. Validate that the script file exists.
3. Execute the script file with powershell -NoProfile -ExecutionPolicy Bypass -File.
4. Validate outputs.
5. Never run the internal body manually line by line.

----------
18. TRY/CATCH/FINALLY ATOMICITY RULE
----------

try/catch/finally must be executed as one atomic block.

Never instruct the user to execute finally separately.

Invalid operational pattern:

try {
  ...
}

Then later:

finally {
  Stop-Transcript
}

This produces:

finally: The term 'finally' is not recognized...

Correct pattern:

Place try/catch/finally inside a saved .ps1 file or provide a single complete paste block that cannot be split.

For fragile human workflows, prefer:

try { Stop-Transcript | Out-Null } catch {}

as a standalone cleanup command before a new execution, not as an orphan finally block.

----------
19. NO BASH HEREDOC IN POWERSHELL RULE
----------

Do not use Bash heredoc syntax inside PowerShell:

python - <<'PY'
import sys
PY

This is Bash syntax, not PowerShell syntax.

PowerShell-safe alternatives:

python -c "import sys; print(sys.version)"

or write a temporary .py file and execute:

Set-Content -LiteralPath "SyS\Temp\script.py" -Value $PythonLines -Encoding UTF8
python "SyS\Temp\script.py"

----------
20. FALSE OK PROHIBITION
----------

Never accept a final STATUS OK line if earlier output contains:

- Cannot bind argument because it is null.
- The term '<function>' is not recognized.
- ParserError.
- Unexpected token.
- catch/finally not recognized.
- Missing output.
- Failed native command.

A valid OK requires:

1. All critical variables are non-null.
2. The main command executed.
3. The expected artifact exists.
4. The log path exists.
5. Output validation confirms file sizes or command results.
6. No unhandled error appears before the summary.

If any of these fail, mark the minibattle as FAILED or RECOVERY_REQUIRED. Do not progress the backlog based on a printed OK alone.

----------
21. ACCEPTANCE CRITERIA ADDENDUM
----------

A PowerShell one-shot involving generated scripts complies with this skill only when:

1. It does not depend on fragile nested here-string delimiters.
2. It writes long generated scripts to a .ps1 file before execution.
3. It validates script creation before execution.
4. It does not mix Bash heredoc syntax into PowerShell.
5. It prevents orphan try/catch/finally blocks.
6. It treats manually printed OK lines as insufficient evidence.
7. It validates outputs using Test-Path/Get-Item or equivalent.
8. It emits clear START/END markers.
9. It tells the human exactly from where to copy terminal output.
10. It avoids continuing after null critical paths.

========== SKILL UPDATE MB-EXAM-005R2 POWERSHELL WRAPPER SAFETY END ==========


==========
MB-GRC-032C_TEMP_ONESHOT_EXECUTION_PROTOCOL
==========
DATE..............: 2026-06-15 11:16:31 -04:00
STATUS............: ACTIVE
TYPE..............: execution_protocol
SCOPE.............: one-shot scripts, temporary executable scripts, repository automation, recovery and escalation

==========
00.00_PRINCIPLE
==========

Do not avoid long one-shot scripts when the objective is fast controlled progress.

Govern them.

The first execution phase may use a large, comprehensive script when the task requires coordinated filesystem, validation, continuity, RADAR, commit and push operations.

The long script must not be pasted directly into the terminal.

It must be saved as a temporary executable file and run through a short activation wrapper.

==========
01.00_STANDARD_PATHS
==========

PowerShell:

SyS/Temp/<MB_ID>.ps1

WSL2 Bash:

SyS/Temp/<MB_ID>.sh

Forbidden:

- root-level MB scripts;
- stale generic TempScript execution without MB_ID guard;
- direct paste of long scripts into terminal;
- script-generated temporary files outside SyS/Temp/ unless explicitly intended and versioned.

==========
02.00_ACTIVATION_WRAPPER
==========

The activation wrapper must be short.

It must:

1. set the repository root;
2. define RUN_ID / MB_ID;
3. verify that the target script exists under SyS/Temp/;
4. verify that the script contains the expected MB_ID;
5. reject root-level temporary scripts;
6. normalize encoding when appropriate;
7. copy the script to the system temp directory;
8. execute the copy;
9. save a timestamped log under SyS/Temp/;
10. print the log;
11. print RUN_EXIT, HEAD_AFTER, STATUS_AFTER.

==========
03.00_SCRIPT_OUTPUT_CONTRACT
==========

Every long one-shot script must print a self-contained block:

========== <MB_ID> / START ==========
...
========== <MB_ID> / END ==========

The block must include:

- local time;
- root path;
- purpose;
- COPY_FROM_HERE: YES;
- precheck;
- current branch and HEAD;
- git status before;
- main actions performed;
- validation result;
- RADAR result when relevant;
- staged files;
- commit hash or explicit no-commit reason;
- push result or explicit no-push reason;
- readiness summary when relevant;
- git status after;
- final status;
- next action.

The user should only need to copy the START/END block back to the assistant.

==========
04.00_ERROR_POLICY
==========

If the first large script fails, do not continue blindly.

The next attempt must reduce scope or isolate the failing stage.

Maximum assisted repair sequence:

- Attempt 1: comprehensive one-shot for fast progress.
- Attempt 2: reduced diagnostic or targeted recovery script.
- Attempt 3: second reduced recovery / final local isolation.
- After third persistent failure: escalate to Codex Desktop or Claude Code Desktop.

The escalation agent must receive:

- root path;
- failing MB_ID;
- latest START/END block;
- git status;
- expected deliverable;
- explicit boundary of what may be edited;
- validation command;
- expected proof of resolution.

==========
05.00_FAILURE_OUTPUT_REQUIREMENTS
==========

On failure, the script must report:

- failing stage;
- failing command;
- exit code when available;
- dirty/untracked files;
- whether any commit happened;
- whether any push happened;
- what is safe to retry;
- exact next action.

The script must not print a false success line after a failed mandatory stage.

Avoid contradictory lines such as READINESS_EXIT: 0 when readiness failed.

==========
06.00_GIT_POLICY
==========

Before execution:

- repo should be clean unless the purpose is cleanup/recovery;
- if dirty, fail early with status details.

During execution:

- stage only intended files;
- avoid broad git add . unless the task explicitly requires it;
- never stage SyS/Temp/;
- never stage root-level one-shot scripts.

After execution:

- git status must be clean or the script must explain why not;
- readiness should be OK for closeable work;
- unpushed commits must be reported.

==========
07.00_FAST_PROGRESS_POLICY
==========

This protocol supports speed.

The preferred first move for repository automation is a high-coverage one-shot script, not a sequence of small manual commands.

The quality bar is not smallness.

The quality bar is:

- reproducibility;
- controlled scope;
- clear failure boundary;
- START/END observability;
- clean repo at close;
- maximum three assisted repair attempts before escalation.

==========
08.00_MINIMUM_USECASE_INCLUSION
==========

This protocol must be present in the operational content for at least:

- 90.USECASE/01.NEW_PROJECT
- 90.USECASE/04.REPOSITORY_STRUCTURE_REPAIR

==========
END_MB-GRC-032C_TEMP_ONESHOT_EXECUTION_PROTOCOL
==========

========== SKILL UPDATE MB-GRC-035B POWERSHELL LONG PAYLOAD PARSE GATE BEGIN ==========

20. LONG PAYLOAD AND PARSE GATE RULE

20.1 LESSON LEARNED

A PowerShell script can pass the operational intent but fail before execution when the generated .ps1 file contains a broken string, missing here-string terminator, truncated payload, accidental variable expansion, or fragile embedded interpolation.

Observed failure signals include:
- ParserError.
- The string is missing the terminator.
- Interactive secondary prompt shown as >>.
- A launcher succeeds but the generated child .ps1 fails to parse.
- A long document payload is truncated before the closing delimiter.
- A literal PowerShell variable such as , , , , or  disappears from the generated document because it was expanded too early.

20.2 HARD RULE

For non-trivial PowerShell work, especially when creating or modifying files, the AI must not rely on direct interactive paste of a long script or on long document payloads embedded in here-strings.

The required pattern is:
1. Generate a temporary .ps1 file under SyS/Temp/<MB_ID>.ps1.
2. Generate script content as an array/list of lines when the script contains long document payloads.
3. Run a parser gate with System.Management.Automation.Language.Parser.ParseFile before execution.
4. If parser errors exist, print them and do not execute the child script.
5. Execute the child script with pwsh.exe -NoProfile -ExecutionPolicy Bypass -File.
6. The launcher must report SCRIPT_EXIT or equivalent and must not close the terminal.
7. The child script must print START, END, FINAL_STATUS, LOG_PATH, failure cause, and next action.

20.3 DOCUMENT PAYLOAD RULE

When a PowerShell script creates a long report, brief, Markdown document, prompt, or policy block, prefer string arrays or a List[string] with one line per item.

Correct literal-safe examples:
-  = New-Object System.Collections.Generic.List[string]
- .Add("one logical output line")
- Set-Content -Path <target> -Value  -Encoding UTF8

Avoid long embedded blocks such as:
-  = double-quoted here-string with a long payload
-  = single-quoted here-string with a long payload
- nested here-strings inside launcher-generated scripts
- complex .Replace() or  interpolation inside Write-Host strings

20.4 LITERAL VARIABLE RULE

When the generated document must contain a literal PowerShell variable, do not write that document line with a double-quoted string in the writer script.

Unsafe writer pattern:
- ========== SKILL UPDATE MB-GRC-035B POWERSHELL LONG PAYLOAD PARSE GATE BEGIN ==========  20. LONG PAYLOAD AND PARSE GATE RULE  20.1 LESSON LEARNED  A PowerShell script can pass the operational intent but fail before execution when the generated .ps1 file contains a broken string, missing here-string terminator, truncated payload, accidental variable expansion, or fragile embedded interpolation.  Observed failure signals include: - ParserError. - The string is missing the terminator. - Interactive secondary prompt shown as >>. - A launcher succeeds but the generated child .ps1 fails to parse. - A long document payload is truncated before the closing delimiter. - A literal PowerShell variable such as , , , , or  disappears from the generated document because it was expanded too early.  20.2 HARD RULE  For non-trivial PowerShell work, especially when creating or modifying files, the AI must not rely on direct interactive paste of a long script or on long document payloads embedded in here-strings.  The required pattern is: 1. Generate a temporary .ps1 file under SyS/Temp/<MB_ID>.ps1. 2. Generate script content as an array/list of lines when the script contains long document payloads. 3. Run a parser gate with System.Management.Automation.Language.Parser.ParseFile before execution. 4. If parser errors exist, print them and do not execute the child script. 5. Execute the child script with pwsh.exe -NoProfile -ExecutionPolicy Bypass -File. 6. The launcher must report SCRIPT_EXIT or equivalent and must not close the terminal. 7. The child script must print START, END, FINAL_STATUS, LOG_PATH, failure cause, and next action.  20.3 DOCUMENT PAYLOAD RULE  When a PowerShell script creates a long report, brief, Markdown document, prompt, or policy block, prefer string arrays or a List[string] with one line per item.  Correct literal-safe examples: -  = New-Object System.Collections.Generic.List[string] - .Add("one logical output line") - Set-Content -Path <target> -Value  -Encoding UTF8  Avoid long embedded blocks such as: -  = double-quoted here-string with a long payload -  = single-quoted here-string with a long payload - nested here-strings inside launcher-generated scripts - complex .Replace() or  interpolation inside Write-Host strings  20.4 LITERAL VARIABLE RULE  When the generated document must contain a literal PowerShell variable, do not write that document line with a double-quoted string in the writer script.  Unsafe writer pattern:.Add("Set-Content -Path <target> -Value  -Encoding UTF8")

Safe writer patterns:
- ========== SKILL UPDATE MB-GRC-035B POWERSHELL LONG PAYLOAD PARSE GATE BEGIN ==========  20. LONG PAYLOAD AND PARSE GATE RULE  20.1 LESSON LEARNED  A PowerShell script can pass the operational intent but fail before execution when the generated .ps1 file contains a broken string, missing here-string terminator, truncated payload, accidental variable expansion, or fragile embedded interpolation.  Observed failure signals include: - ParserError. - The string is missing the terminator. - Interactive secondary prompt shown as >>. - A launcher succeeds but the generated child .ps1 fails to parse. - A long document payload is truncated before the closing delimiter. - A literal PowerShell variable such as , , , , or  disappears from the generated document because it was expanded too early.  20.2 HARD RULE  For non-trivial PowerShell work, especially when creating or modifying files, the AI must not rely on direct interactive paste of a long script or on long document payloads embedded in here-strings.  The required pattern is: 1. Generate a temporary .ps1 file under SyS/Temp/<MB_ID>.ps1. 2. Generate script content as an array/list of lines when the script contains long document payloads. 3. Run a parser gate with System.Management.Automation.Language.Parser.ParseFile before execution. 4. If parser errors exist, print them and do not execute the child script. 5. Execute the child script with pwsh.exe -NoProfile -ExecutionPolicy Bypass -File. 6. The launcher must report SCRIPT_EXIT or equivalent and must not close the terminal. 7. The child script must print START, END, FINAL_STATUS, LOG_PATH, failure cause, and next action.  20.3 DOCUMENT PAYLOAD RULE  When a PowerShell script creates a long report, brief, Markdown document, prompt, or policy block, prefer string arrays or a List[string] with one line per item.  Correct literal-safe examples: -  = New-Object System.Collections.Generic.List[string] - .Add("one logical output line") - Set-Content -Path <target> -Value  -Encoding UTF8  Avoid long embedded blocks such as: -  = double-quoted here-string with a long payload -  = single-quoted here-string with a long payload - nested here-strings inside launcher-generated scripts - complex .Replace() or  interpolation inside Write-Host strings  20.4 LITERAL VARIABLE RULE  When the generated document must contain a literal PowerShell variable, do not write that document line with a double-quoted string in the writer script.  Unsafe writer pattern: - ========== SKILL UPDATE MB-GRC-035B POWERSHELL LONG PAYLOAD PARSE GATE BEGIN ==========  20. LONG PAYLOAD AND PARSE GATE RULE  20.1 LESSON LEARNED  A PowerShell script can pass the operational intent but fail before execution when the generated .ps1 file contains a broken string, missing here-string terminator, truncated payload, accidental variable expansion, or fragile embedded interpolation.  Observed failure signals include: - ParserError. - The string is missing the terminator. - Interactive secondary prompt shown as >>. - A launcher succeeds but the generated child .ps1 fails to parse. - A long document payload is truncated before the closing delimiter. - A literal PowerShell variable such as , , , , or  disappears from the generated document because it was expanded too early.  20.2 HARD RULE  For non-trivial PowerShell work, especially when creating or modifying files, the AI must not rely on direct interactive paste of a long script or on long document payloads embedded in here-strings.  The required pattern is: 1. Generate a temporary .ps1 file under SyS/Temp/<MB_ID>.ps1. 2. Generate script content as an array/list of lines when the script contains long document payloads. 3. Run a parser gate with System.Management.Automation.Language.Parser.ParseFile before execution. 4. If parser errors exist, print them and do not execute the child script. 5. Execute the child script with pwsh.exe -NoProfile -ExecutionPolicy Bypass -File. 6. The launcher must report SCRIPT_EXIT or equivalent and must not close the terminal. 7. The child script must print START, END, FINAL_STATUS, LOG_PATH, failure cause, and next action.  20.3 DOCUMENT PAYLOAD RULE  When a PowerShell script creates a long report, brief, Markdown document, prompt, or policy block, prefer string arrays or a List[string] with one line per item.  Correct literal-safe examples: -  = New-Object System.Collections.Generic.List[string] - .Add("one logical output line") - Set-Content -Path <target> -Value  -Encoding UTF8  Avoid long embedded blocks such as: -  = double-quoted here-string with a long payload -  = single-quoted here-string with a long payload - nested here-strings inside launcher-generated scripts - complex .Replace() or  interpolation inside Write-Host strings  20.4 LITERAL VARIABLE RULE  When the generated document must contain a literal PowerShell variable, do not write that document line with a double-quoted string in the writer script.  Unsafe writer pattern:.Add("Set-Content -Path <target> -Value  -Encoding UTF8")  Safe writer patterns:.Add('Set-Content -Path <target> -Value  -Encoding UTF8')
- ========== SKILL UPDATE MB-GRC-035B POWERSHELL LONG PAYLOAD PARSE GATE BEGIN ==========  20. LONG PAYLOAD AND PARSE GATE RULE  20.1 LESSON LEARNED  A PowerShell script can pass the operational intent but fail before execution when the generated .ps1 file contains a broken string, missing here-string terminator, truncated payload, accidental variable expansion, or fragile embedded interpolation.  Observed failure signals include: - ParserError. - The string is missing the terminator. - Interactive secondary prompt shown as >>. - A launcher succeeds but the generated child .ps1 fails to parse. - A long document payload is truncated before the closing delimiter. - A literal PowerShell variable such as , , , , or  disappears from the generated document because it was expanded too early.  20.2 HARD RULE  For non-trivial PowerShell work, especially when creating or modifying files, the AI must not rely on direct interactive paste of a long script or on long document payloads embedded in here-strings.  The required pattern is: 1. Generate a temporary .ps1 file under SyS/Temp/<MB_ID>.ps1. 2. Generate script content as an array/list of lines when the script contains long document payloads. 3. Run a parser gate with System.Management.Automation.Language.Parser.ParseFile before execution. 4. If parser errors exist, print them and do not execute the child script. 5. Execute the child script with pwsh.exe -NoProfile -ExecutionPolicy Bypass -File. 6. The launcher must report SCRIPT_EXIT or equivalent and must not close the terminal. 7. The child script must print START, END, FINAL_STATUS, LOG_PATH, failure cause, and next action.  20.3 DOCUMENT PAYLOAD RULE  When a PowerShell script creates a long report, brief, Markdown document, prompt, or policy block, prefer string arrays or a List[string] with one line per item.  Correct literal-safe examples: -  = New-Object System.Collections.Generic.List[string] - .Add("one logical output line") - Set-Content -Path <target> -Value  -Encoding UTF8  Avoid long embedded blocks such as: -  = double-quoted here-string with a long payload -  = single-quoted here-string with a long payload - nested here-strings inside launcher-generated scripts - complex .Replace() or  interpolation inside Write-Host strings  20.4 LITERAL VARIABLE RULE  When the generated document must contain a literal PowerShell variable, do not write that document line with a double-quoted string in the writer script.  Unsafe writer pattern: - ========== SKILL UPDATE MB-GRC-035B POWERSHELL LONG PAYLOAD PARSE GATE BEGIN ==========  20. LONG PAYLOAD AND PARSE GATE RULE  20.1 LESSON LEARNED  A PowerShell script can pass the operational intent but fail before execution when the generated .ps1 file contains a broken string, missing here-string terminator, truncated payload, accidental variable expansion, or fragile embedded interpolation.  Observed failure signals include: - ParserError. - The string is missing the terminator. - Interactive secondary prompt shown as >>. - A launcher succeeds but the generated child .ps1 fails to parse. - A long document payload is truncated before the closing delimiter. - A literal PowerShell variable such as , , , , or  disappears from the generated document because it was expanded too early.  20.2 HARD RULE  For non-trivial PowerShell work, especially when creating or modifying files, the AI must not rely on direct interactive paste of a long script or on long document payloads embedded in here-strings.  The required pattern is: 1. Generate a temporary .ps1 file under SyS/Temp/<MB_ID>.ps1. 2. Generate script content as an array/list of lines when the script contains long document payloads. 3. Run a parser gate with System.Management.Automation.Language.Parser.ParseFile before execution. 4. If parser errors exist, print them and do not execute the child script. 5. Execute the child script with pwsh.exe -NoProfile -ExecutionPolicy Bypass -File. 6. The launcher must report SCRIPT_EXIT or equivalent and must not close the terminal. 7. The child script must print START, END, FINAL_STATUS, LOG_PATH, failure cause, and next action.  20.3 DOCUMENT PAYLOAD RULE  When a PowerShell script creates a long report, brief, Markdown document, prompt, or policy block, prefer string arrays or a List[string] with one line per item.  Correct literal-safe examples: -  = New-Object System.Collections.Generic.List[string] - .Add("one logical output line") - Set-Content -Path <target> -Value  -Encoding UTF8  Avoid long embedded blocks such as: -  = double-quoted here-string with a long payload -  = single-quoted here-string with a long payload - nested here-strings inside launcher-generated scripts - complex .Replace() or  interpolation inside Write-Host strings  20.4 LITERAL VARIABLE RULE  When the generated document must contain a literal PowerShell variable, do not write that document line with a double-quoted string in the writer script.  Unsafe writer pattern:.Add("Set-Content -Path <target> -Value  -Encoding UTF8")  Safe writer patterns: - ========== SKILL UPDATE MB-GRC-035B POWERSHELL LONG PAYLOAD PARSE GATE BEGIN ==========  20. LONG PAYLOAD AND PARSE GATE RULE  20.1 LESSON LEARNED  A PowerShell script can pass the operational intent but fail before execution when the generated .ps1 file contains a broken string, missing here-string terminator, truncated payload, accidental variable expansion, or fragile embedded interpolation.  Observed failure signals include: - ParserError. - The string is missing the terminator. - Interactive secondary prompt shown as >>. - A launcher succeeds but the generated child .ps1 fails to parse. - A long document payload is truncated before the closing delimiter. - A literal PowerShell variable such as , , , , or  disappears from the generated document because it was expanded too early.  20.2 HARD RULE  For non-trivial PowerShell work, especially when creating or modifying files, the AI must not rely on direct interactive paste of a long script or on long document payloads embedded in here-strings.  The required pattern is: 1. Generate a temporary .ps1 file under SyS/Temp/<MB_ID>.ps1. 2. Generate script content as an array/list of lines when the script contains long document payloads. 3. Run a parser gate with System.Management.Automation.Language.Parser.ParseFile before execution. 4. If parser errors exist, print them and do not execute the child script. 5. Execute the child script with pwsh.exe -NoProfile -ExecutionPolicy Bypass -File. 6. The launcher must report SCRIPT_EXIT or equivalent and must not close the terminal. 7. The child script must print START, END, FINAL_STATUS, LOG_PATH, failure cause, and next action.  20.3 DOCUMENT PAYLOAD RULE  When a PowerShell script creates a long report, brief, Markdown document, prompt, or policy block, prefer string arrays or a List[string] with one line per item.  Correct literal-safe examples: -  = New-Object System.Collections.Generic.List[string] - .Add("one logical output line") - Set-Content -Path <target> -Value  -Encoding UTF8  Avoid long embedded blocks such as: -  = double-quoted here-string with a long payload -  = single-quoted here-string with a long payload - nested here-strings inside launcher-generated scripts - complex .Replace() or  interpolation inside Write-Host strings  20.4 LITERAL VARIABLE RULE  When the generated document must contain a literal PowerShell variable, do not write that document line with a double-quoted string in the writer script.  Unsafe writer pattern: - ========== SKILL UPDATE MB-GRC-035B POWERSHELL LONG PAYLOAD PARSE GATE BEGIN ==========  20. LONG PAYLOAD AND PARSE GATE RULE  20.1 LESSON LEARNED  A PowerShell script can pass the operational intent but fail before execution when the generated .ps1 file contains a broken string, missing here-string terminator, truncated payload, accidental variable expansion, or fragile embedded interpolation.  Observed failure signals include: - ParserError. - The string is missing the terminator. - Interactive secondary prompt shown as >>. - A launcher succeeds but the generated child .ps1 fails to parse. - A long document payload is truncated before the closing delimiter. - A literal PowerShell variable such as , , , , or  disappears from the generated document because it was expanded too early.  20.2 HARD RULE  For non-trivial PowerShell work, especially when creating or modifying files, the AI must not rely on direct interactive paste of a long script or on long document payloads embedded in here-strings.  The required pattern is: 1. Generate a temporary .ps1 file under SyS/Temp/<MB_ID>.ps1. 2. Generate script content as an array/list of lines when the script contains long document payloads. 3. Run a parser gate with System.Management.Automation.Language.Parser.ParseFile before execution. 4. If parser errors exist, print them and do not execute the child script. 5. Execute the child script with pwsh.exe -NoProfile -ExecutionPolicy Bypass -File. 6. The launcher must report SCRIPT_EXIT or equivalent and must not close the terminal. 7. The child script must print START, END, FINAL_STATUS, LOG_PATH, failure cause, and next action.  20.3 DOCUMENT PAYLOAD RULE  When a PowerShell script creates a long report, brief, Markdown document, prompt, or policy block, prefer string arrays or a List[string] with one line per item.  Correct literal-safe examples: -  = New-Object System.Collections.Generic.List[string] - .Add("one logical output line") - Set-Content -Path <target> -Value  -Encoding UTF8  Avoid long embedded blocks such as: -  = double-quoted here-string with a long payload -  = single-quoted here-string with a long payload - nested here-strings inside launcher-generated scripts - complex .Replace() or  interpolation inside Write-Host strings  20.4 LITERAL VARIABLE RULE  When the generated document must contain a literal PowerShell variable, do not write that document line with a double-quoted string in the writer script.  Unsafe writer pattern:.Add("Set-Content -Path <target> -Value  -Encoding UTF8")  Safe writer patterns:.Add('Set-Content -Path <target> -Value  -Encoding UTF8').Add("Set-Content -Path <target> -Value $Lines -Encoding UTF8")

20.5 PARSE GATE MINIMUM

Before running a generated .ps1 file, the launcher must parse it:

$Tokens = $null
$ParseErrors = $null
[System.Management.Automation.Language.Parser]::ParseFile($ScriptPath, [ref]$Tokens, [ref]$ParseErrors) | Out-Null
if ($ParseErrors.Count -gt 0) { print errors; do not execute }

20.6 RECOVERY RULE

If the human sees the interactive secondary prompt >>, the human must not keep pasting. The correct recovery is:
1. Press Ctrl+C.
2. Confirm return to PS <repo path>.
3. Run git status --short.
4. Re-run only through a saved .ps1 file with parser gate.

20.7 DONE

A PowerShell long-script delivery is acceptable only when:
- the generated .ps1 exists under SyS/Temp/;
- ParseFile returns zero parser errors;
- the launcher prints the child script exit status;
- the child script prints FINAL_STATUS;
- git status is checked before and after;
- changed files are staged explicitly;
- SyS/Temp/ is never staged.

========== SKILL UPDATE MB-GRC-035B POWERSHELL LONG PAYLOAD PARSE GATE END ==========

========== MB-SM-046B_POWERSHELL_METHOD_STANDARD ==========
TITLE=PowerShell repository workflow method
STATUS=ACTIVE
SCOPE:
- Applies to PowerShell scripts generated for this repository and for SkillsMachine-style project/repository operations.
- Applies especially to downloadable .ps1 scripts copied to SyS\Temp and executed by the human operator.

MANDATORY_METHOD:
1. Prefer downloadable scripts for long or fragile PowerShell operations.
2. Generate the script as an artifact; the human copies it to C:\01. GitHub\Skills\SyS\Temp and executes it from there.
3. Every script that writes outputs to SyS\Temp must clean SyS\Temp first while preserving itself when it is executing from that folder.
4. Every long-running scan or mutation script must use Write-Progress with percent-complete status.
5. Every repository-mutating script must hard-stop before mutation unless git status --short is clean.
6. Mutating scripts must use explicit allowlists and forbidden-path checks for broad transformations.
7. Scripts must not commit or push. They may produce a commit-ready gate; the human commits manually.
8. Scripts must run Validate-System.ps1 after mutation when validation is available.
9. Validate-System.ps1 includes Test-PowerShellSyntax.ps1; new .ps1 files must parse before commit.
10. Avoid parser-prone constructs. Prefer explicit hashtable sort expressions over abbreviated multi-key Sort-Object syntax.
11. Do not use broad repository transforms such as git add --renormalize . unless explicitly authorized for that minibattle.
12. For line-ending work, separate core live files from backups, logs, generated bundles, and labs before any apply step.

AI_TAIL_REQUIREMENT:
- Print exactly five blank lines before AI_TAIL_START.
- Emit AI_TAIL_START and AI_TAIL_END.
- Include at minimum: AI_TAIL_SCHEMA, MB_ID, FINAL_STATUS, ROOT, OUTPUT_TEMP, CANON_MODIFIED, COMMIT_PUSH_PERFORMED, VALIDATE_STATUS when applicable, and NEXT_ACTION.
==========

==========
MB-SM-054B_P0_POWERSHELL_RUNNER_HARDENING_RULE
==========
DATE_LOCAL........: 2026-07-04
STATUS............: ACTIVE_RULE
LAYER.............: POWERSHELL_INTERACTIVE_SAFETY
SOURCE_EVIDENCE...: CloseReport PowerShell/openpyxl/.Count failures, MB-SM-054A
PURPOSE...........:
Prevent recurring PowerShell runner failures in VS Code terminal workflows.

ARRAY_AND_COUNT_RULE:
When a value may be scalar, null, or array, wrap it before using Count:
$items = @(<expression>)
Then use $items.Count.

Do not assume .Count exists on arbitrary objects.

PYTHON_CHILD_PROCESS_RULE:
When running Python from PowerShell, especially with openpyxl or libraries that may write warnings:
- avoid 2>&1 | Tee-Object for required child process execution;
- prefer Start-Process with RedirectStandardOutput and RedirectStandardError;
- capture exit code explicitly;
- inspect stderr separately;
- filter known non-blocking warnings only when intentionally documented;
- fail on unexpected stderr if the command is required.

OPENPYXL_WARNING_RULE:
Known warning pattern:
"Conditional Formatting extension is not supported and will be removed"

This may be non-blocking for read-only analysis, but must not be allowed to become a PowerShell NativeCommandError through unsafe redirection.

FALSE_PASS_RULE:
If child process exit code is non-zero, or if expected output files are missing, parent PowerShell runner must fail.

INTERACTIVE_PROMPT_RULE:
If PowerShell enters continuation prompt >>, stop with Ctrl+C and do not keep pasting.
Treat it as unclosed string/block/parenthesis.

DONE_WHEN:
Runner scripts survive scalar/array ambiguity, Python warning noise, stderr capture and child-process failure propagation.
==========
FIN_MB-SM-054B_P0_POWERSHELL_RUNNER_HARDENING_RULE
==========


==========
MB-SM-067B-D2_POWERSHELL_STRICTMODE_AND_COLLECTION_ANTIPATTERNS
==========
DATE..............: 2026-07-19
STATUS............: ACTIVE_RULE
LAYER.............: POWERSHELL_INTERACTIVE_SAFETY
SOURCE_MB.........: MB-SM-067B-D2_SKILL_GRC_PROMOTION_PATCH
SOURCE_EVIDENCE...: Three real incidents from MB-SM-067B audits plus MB-SM-067B-D1 module hardening
MERGES............: SKILL.PowerShellScriptHardeningForExcelCOM (candidate, partial; Excel COM specifics stay CloseReport-local)

22. STRICTMODE AND COLLECTION ANTI-PATTERNS (HARD RULES)

22.1 ANTI-PATTERN: INLINE IF-EXPRESSION AS -f ARGUMENT
Never pass an inline if expression as an argument to the -f format operator:
  UNSAFE:  "{0}" -f (if ($x) { 'a' } else { 'b' })     # parser precedence failure
  SAFE:    $val = if ($x) { 'a' } else { 'b' }
           "{0}" -f $val
Assign conditionals to a variable first; format with the variable.

22.2 ANTI-PATTERN: @(Invoke-*).Property
Never wrap a function call in @() and then access a property on the wrapper:
  UNSAFE:  @(Invoke-Something).ExitCode      # property of the array, not the object
  SAFE:    $result = Invoke-Something
           $result.ExitCode
Array-wrap only when you need an array; access properties on the captured object.

22.3 SCALAR/ARRAY UNWRAPPING AND EMPTY-ARRAY-TO-NULL
PowerShell pipelines unwrap results: one object arrives as a scalar, and an
empty @() returned from a function arrives as $null at the caller.
Consequences under Set-StrictMode -Version Latest:
- $null.Count throws PropertyNotFoundException;
- .Count on some typed CLR arrays is not directly accessible.
Rules:
- Wrap ambiguous values before counting: $items = @(<expr>); $items.Count
- Use .Length for typed arrays when applicable.
- Never assume a function that "returns an array" cannot deliver $null.

22.4 COLLECTION NORMALIZATION VIA List[T].ToArray()
When a function must ALWAYS return a non-null array (even when empty), build a
System.Collections.Generic.List[T] and return $list.ToArray() assigned directly:
  $list = [System.Collections.Generic.List[string]]::new()
  foreach ($x in $input) { $list.Add($x) | Out-Null }
  $result = [string[]]$list.ToArray()
A direct .NET method call assignment is not pipeline-unwrapped, so empty
collections stay as empty arrays instead of becoming $null.

22.5 STRICTMODE PROPERTY GUARDS
Under Set-StrictMode -Version Latest, accessing an absent property throws.
Guard optional property access on PSCustomObjects (e.g. parsed JSON):
  if ($null -ne $obj.PSObject.Properties['name']) { $obj.name }
Never access schema-optional JSON properties without a guard.

22.6 PARSER GATE PLUS FUNCTIONAL PROBE
A parser gate (Language.Parser::ParseFile with zero errors) is necessary but NOT
sufficient: all three real incidents above parsed cleanly and failed at runtime.
For non-trivial scripts, additionally run at least one representative functional
probe (a happy-path invocation validating real output) before declaring the
script safe for delivery.

22.7 PREFER THE COMMON AUTOMATION MODULE
For SkillsMachine repository automation, prefer the tested helpers in
SyS/A_Tools/Common/SkillsMachine.Automation.psm1 (Invoke-GitSafe, Get-GitStatus,
Get-RepoIdentity, Assert-ProjectContext, Assert-WorktreeClean, Read-JsonSafe,
Get-UseCaseRegistryModel, Test-PowerShellFileSyntax, Write-Utf8NoBom, Get-Sha256,
Test-ObjectProperty, New-AiTail) over ad-hoc copied helper code. The module runs
under StrictMode with a Pester regression suite that encodes these anti-patterns.

FIN_MB-SM-067B-D2_POWERSHELL_STRICTMODE_AND_COLLECTION_ANTIPATTERNS
==========
