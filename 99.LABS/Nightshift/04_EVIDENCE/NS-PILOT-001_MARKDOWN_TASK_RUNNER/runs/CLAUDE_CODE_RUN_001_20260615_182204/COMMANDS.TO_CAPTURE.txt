COMMANDS TO CAPTURE DURING CLAUDE CODE RUN

After Claude Code modifies files, capture:

git status --short
git diff --stat
python --version
python -m unittest discover -s 99.LABS/Nightshift/03_EXPERIMENTS/NS-PILOT-001_MARKDOWN_TASK_RUNNER/tests

If Claude Code creates a CLI, also capture the command that generates the report.

Do not commit Claude Code changes until human review.
